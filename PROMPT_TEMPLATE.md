# 算法题目总结撰写提示词模板

## 快速使用

复制以下内容作为提示词开头：

```markdown
请为以下题目撰写总结文档。

## 必须遵守的规范

### 结构要求
1. **渐进式讲解**：必须按 暴力解法 → 优化解法 → 最优解法 的顺序
2. **详细解释**：每个关键步骤都要解释"为什么"
3. **示例推演**：给出具体数字的完整推演过程，一步一步展示

### 绝对禁止
以下表达严禁出现：
- "让我重新推演"
- "等等，这里出现了"
- "实际上这个判断有误"
- "正确的应该是"
- "仔细检查后发现"
- "不对，应该是"
- "修正一下"

### 代码要求
- 使用 `float('-inf')` 和 `float('inf')` 简化边界处理
- 避免大量 if-else 分支
- 包含类型注解

### 格式要求
- 复杂度分析用表格对比
- 列出易错点
- 包含扩展思考

## 自查清单（完成后检查）
- [ ] 没有思考痕迹？
- [ ] 示例一步到位？
- [ ] 代码简洁优雅？
- [ ] 面向初学者？

**重要：文档是最终产品，不是草稿。有任何犹豫或修正，在输出前全部删除，只保留最终正确的版本。**

---

**题目**：
```

## 完整示例

```markdown
请为以下题目撰写总结文档。

## 必须遵守的规范

### 结构要求
1. **渐进式讲解**：必须按 暴力解法 → 优化解法 → 最优解法 的顺序
2. **详细解释**：每个关键步骤都要解释"为什么"
3. **示例推演**：给出具体数字的完整推演过程，一步一步展示

### 绝对禁止
以下表达严禁出现：
- "让我重新推演"
- "等等，这里出现了"
- "实际上这个判断有误"
- "正确的应该是"
- "仔细检查后发现"
- "不对，应该是"
- "修正一下"

### 代码要求
- 使用 `float('-inf')` 和 `float('inf')` 简化边界处理
- 避免大量 if-else 分支
- 包含类型注解

### 格式要求
- 复杂度分析用表格对比
- 列出易错点
- 包含扩展思考

## 自查清单（完成后检查）
- [ ] 没有思考痕迹？
- [ ] 示例一步到位？
- [ ] 代码简洁优雅？
- [ ] 面向初学者？

**重要：文档是最终产品，不是草稿。有任何犹豫或修正，在输出前全部删除，只保留最终正确的版本。**

---

**题目**：两数之和
给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出和为目标值 target 的那两个整数，并返回它们的数组下标。

**要求**：
- 时间复杂度 O(n)
- 空间复杂度 O(n)
```

## 使用方式

1. 复制上方的"快速使用"模板
2. 填入具体题目内容
3. 发送给 AI
4. AI 输出后，对照自查清单检查
5. 如果有问题，要求 AI 修改

## 检查脚本（可选）

可以用以下命令快速检查文档是否有思考痕迹：

```bash
# 检查禁用词汇
grep -E "(让我|等等|实际上|正确的应该是|仔细检查|不对.*应该|修正一下|重新检查)" docs/problems/lc/*.md
```
